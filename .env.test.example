# MySQL test connection DSN (without db name; db name _test_flow is appended by code automatically)
# Note: do NOT append a trailing / (code appends /dbName automatically)
# Format: user:pass@tcp(host:port)
TEST_MYSQL_DSN=root:123456@tcp(127.0.0.1:3306)

# Redis test connection
TEST_REDIS_ADDR=127.0.0.1:6379
TEST_REDIS_PASSWORD=123456
